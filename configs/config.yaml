
project: defect-detection
data:
  train_dir: data/sample_images
  input_size: [512, 512]
model:
  type: unet
  backbone: tiny
training:
  epochs: 10
  batch_size: 8
  lr: 1e-3
paths:
  checkpoint_dir: runs
